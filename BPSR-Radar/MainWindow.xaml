<Window x:Class="BpsrRadar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BpsrRadar"
        Title="BPSR Radar"
        Width="340" Height="420"
        MinWidth="270" MinHeight="320"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">
    <Window.Resources>
        <Style x:Key="TitleBtn" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#BFE3E6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A3F44"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border x:Name="RootBorder" CornerRadius="12" BorderThickness="1"
            BorderBrush="#33555A5F">
        <Border.Background>
            <SolidColorBrush x:Name="RootBrush" Color="#FF0A1418" Opacity="0.9"/>
        </Border.Background>
        <Grid>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title bar -->
                <Grid x:Name="TitleBar" Height="38" Margin="8,4,8,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="BPSR Radar" VerticalAlignment="Center" Foreground="#CFE8EA"
                               FontSize="12" FontWeight="SemiBold"/>
                    <Button x:Name="PinButton" Grid.Column="1" Style="{StaticResource TitleBtn}" Content="📌"/>
                    <Button x:Name="ThroughButton" Grid.Column="2" Style="{StaticResource TitleBtn}" Content="🖱"/>
                    <Button x:Name="LockButton" Grid.Column="3" Style="{StaticResource TitleBtn}" Content="🎯"/>
                    <Button x:Name="SettingsButton" Grid.Column="4" Style="{StaticResource TitleBtn}" Content="⚙"/>
                    <Button x:Name="CloseButton" Grid.Column="5" Style="{StaticResource TitleBtn}" Content="✕"
                            Width="26" Height="24" FontSize="11"/>
                </Grid>

                <!-- Map -->
                <local:RadarCanvas x:Name="MapCanvas" Grid.Row="1" Margin="8,4,8,4" MouseWheel="MapCanvas_MouseWheel"/>

                <!-- Status bar -->
                <Grid Grid.Row="2" Margin="8,0,8,6" Height="32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="#9FC4C8" FontSize="11"
                               TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Column="1" Style="{StaticResource TitleBtn}" Content="−" Click="ZoomOut_Click"/>
                    <Button Grid.Column="2" Style="{StaticResource TitleBtn}" Content="＋" Click="ZoomIn_Click"/>
                    <Button x:Name="FitButton" Grid.Column="3" Style="{StaticResource TitleBtn}" Content="Fit"
                            Width="44" FontSize="11" Click="Fit_Click" Visibility="Collapsed"/>
                </Grid>
            </Grid>

            <!-- Resize handles (WindowStyle=None has no native frame) -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="6"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="6"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="6"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Row="0" Grid.Column="0" Tag="13" Cursor="SizeNWSE" Background="Transparent" MouseLeftButtonDown="ResizeBorder_MouseDown"/>
                <Border Grid.Row="0" Grid.Column="1" Tag="12" Cursor="SizeNS"   Background="Transparent" MouseLeftButtonDown="ResizeBorder_MouseDown"/>
                <Border Grid.Row="0" Grid.Column="2" Tag="14" Cursor="SizeNESW" Background="Transparent" MouseLeftButtonDown="ResizeBorder_MouseDown"/>
                <Border Grid.Row="1" Grid.Column="0" Tag="10" Cursor="SizeWE"   Background="Transparent" MouseLeftButtonDown="ResizeBorder_MouseDown"/>
                <Border Grid.Row="1" Grid.Column="2" Tag="11" Cursor="SizeWE"   Background="Transparent" MouseLeftButtonDown="ResizeBorder_MouseDown"/>
                <Border Grid.Row="2" Grid.Column="0" Tag="16" Cursor="SizeNESW" Background="Transparent" MouseLeftButtonDown="ResizeBorder_MouseDown"/>
                <Border Grid.Row="2" Grid.Column="1" Tag="15" Cursor="SizeNS"   Background="Transparent" MouseLeftButtonDown="ResizeBorder_MouseDown"/>
                <Border Grid.Row="2" Grid.Column="2" Tag="17" Cursor="SizeNWSE" Background="Transparent" MouseLeftButtonDown="ResizeBorder_MouseDown"/>
            </Grid>
        </Grid>
    </Border>
</Window>
